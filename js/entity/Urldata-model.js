export class Urldata {
  constructor (id, urlsetid, content, parsed, source, reviewed, note) {
    this.id = id
    this.urlsetid = urlsetid
    this.content = content
    this.parsed = parsed
    this.source = source
    this.reviewed = reviewed
    this.note = note
  }
}
